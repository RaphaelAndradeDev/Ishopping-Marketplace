@model Ishopping.MVC.Models.ForgotPasswordViewModel
@{
    ViewBag.Title = "Redefinição de Senha";
}

<br />
<h2>@ViewBag.Title</h2>

<div class="row">
    <div class="col-sm-8 col-md-7">
        <section id="forgotPassword">
            @using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                @Html.AntiForgeryToken()
                <h4>Forneça seu e-mail.</h4>
                <hr />
                @Html.ValidationSummary("", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(m => m.Email, new { @class = "col-sm-3 col-md-3 col-lg-2 control-label" })
                    <div class="col-sm-8 col-md-8 inputm">
                        @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-offset-3 col-md-offset-3 col-lg-offset-2 col-md-8">
                        <input type="submit" class="btn btn-default" value="Enviar" />
                    </div>
                </div>
            }
        </section>
    </div>
    <div class="col-sm-11 col-md-4">
        <section id="email">
            <h4>Enviaremos um e-mail.</h4>
            <hr />
            <p>
                Enviaremos um e-mail com as instruções para que você possa alterar a senha. Digite o seu e-mail e em
                seguida verifique a sua caixa de entrada. Caso não esteja na caixa de entrada verifique também a caixa de span.
            </p>
        </section>
    </div>
</div>

    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }
